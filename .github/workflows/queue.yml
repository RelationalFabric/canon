name: Queue

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *' # Daily at midnight

jobs:
  all-checks-passed:
    name: All Checks Passed
    runs-on: ubuntu-latest
    # This job runs but immediately succeeds, providing a placeholder status
    # that can be overridden by the PR workflow when it runs

    steps:
      - name: Queue placeholder
        run: echo "Queue placeholder - status will be overridden by PR workflow"
